<!DOCTYPE html>
<html>
  <head>
    <title>Blog</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag 'https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css' %>

    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
  <nav class="navbar navbar-expand-lg navbar-light bg-light">
  <a class="navbar-brand" href="/">BlogApp</a>
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>

  <div class="collapse navbar-collapse" id="navbarSupportedContent">
    <ul class="navbar-nav mr-auto">
    <% if user_signed_in? %>
      <li class="nav-item">
      <%= link_to 'New post', new_post_path,class:'nav-link' %><span class="sr-only">(current)</span>
      </li>
    </ul>
    <div class="form-inline">
      <%= link_to current_user.username, user_path(current_user.id),class:'btn btn-outline-dark' %>
      <%= link_to 'Logout', destroy_user_session_path, method: :delete, class:'btn btn-outline-dark' %>
    <% else %>
      <%= link_to 'Sign up', new_user_registration_path,class:'btn btn-outline-dark'  %>
      <%= link_to 'Login', new_user_session_path,class:'btn btn-outline-dark'  %>
    <% end %>
      </div>
  </div>
</nav>
    <div class="container">
    <%= yield %>
    </div>
  </body>
</html>
